# ===========================================
# Bumicerts Environment Variables
# ===========================================

# -------------------------------------------
# Supabase Configuration
# -------------------------------------------
# Get these from your Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# -------------------------------------------
# OAuth Configuration (ATProto)
# -------------------------------------------
# Base URL of your application
# 
# LOCAL DEVELOPMENT (loopback OAuth - no ngrok needed):
#   - Use http://127.0.0.1:3000 (NOT localhost)
#   - Enables RFC 8252 loopback client flow
#   - OAuth automatically configured for development mode
#   - Just run: bun run dev
#
# PRODUCTION:
#   - Use https://bumicerts.com or your production domain
#   - OAuth uses private_key_jwt authentication
#
# OPTIONAL - ngrok for testing OAuth with external services:
#   - Set to https://your-id.ngrok-free.app
#   - Overrides automatic loopback configuration
#   - Useful for testing webhooks or external integrations
NEXT_PUBLIC_BASE_URL=http://127.0.0.1:3000

# ES256 Private Key in JWK format for OAuth client authentication
# Generate using: node scripts/generate-oauth-key.js
# Must include: kty, crv, x, y, d, kid, use, alg
# Wrapped in a { keys: [...] } object for JWKS compatibility
ATPROTO_JWK_PRIVATE='{"keys":[{"kty":"EC","crv":"P-256","x":"...","y":"...","d":"...","kid":"key-1","use":"sig","alg":"ES256"}]}'

# -------------------------------------------
# Session Configuration
# -------------------------------------------
# Secret key for encrypting session cookies (minimum 32 characters)
COOKIE_SECRET=your-secret-key-at-least-32-characters-long

# Optional: Custom cookie name (defaults to "climateai_session")
COOKIE_NAME=bumicerts_session

# -------------------------------------------
# Vercel Blob Storage
# -------------------------------------------
BLOB_READ_WRITE_TOKEN=

# -------------------------------------------
# Additional Configuration
# -------------------------------------------
# Add any other environment variables your app needs below
# to send invite token emails
RESEND_API_KEY=re_dsafsdkjflsdkfj